'use server';

/**
 * Site assistant flow removed. Ready for a fresh implementation.
 */
export async function askSiteAssistant(input: string): Promise<any> {
  return { responseText: "Chatbot is being reset." };
}
